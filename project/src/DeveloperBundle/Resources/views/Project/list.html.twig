{% extends 'DeveloperBundle::content_page_layout.html.twig' %}
{% set title = 'projects.title'|trans %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <a href="{{ path('developer_projects_new',{'developer': developer.id}) }}"
                       class="btn btn-success"
                    >
                        {{ 'projects.action.create'|trans }}
                    </a>
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="dataTable_wrapper">
                        <table class="table table-striped table-bordered table-hover">
                            <thead>
                            <tr>
                                <th>{{ 'projects.th.id'|trans }}</th>
                                <th>{{ 'projects.th.name'|trans }}</th>
                                <th>{{ 'projects.th.contact.name'|trans }}</th>
                                <th>{{ 'projects.th.contact.number'|trans }}</th>
                                <th>{{ 'projects.th.creation.date'|trans }}</th>
                                <th>{{ 'projects.th.actions'|trans }}</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for project in projects %}
                                <tr>
                                    <td>{{ project.id }}</td>
                                    <td>{{ project.name }}</td>
                                    <td>{{ project.contactName }}</td>
                                    <td>{{ project.contactNumber }}</td>
                                    <td>{{ project.creationDate | date }}</td>
                                    <td>
                                        <a
                                            href="{{ path(
                                                'developer_project_show',
                                                {
                                                    'developer':developer.id,
                                                    'project': project.id
                                                }
                                            )}}"
                                            class="btn btn-sm btn-primary"
                                        >
                                            {{ 'action.show'|trans }}
                                        </a>
                                        <a
                                            href="{{ path(
                                                'developer_project_edit',
                                                {
                                                    'developer': developer.id,
                                                    'project': project.id
                                                }
                                            )}}"
                                            class="btn btn-sm btn-primary"
                                        >
                                            {{ 'action.edit'|trans }}
                                        </a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                    {{ knp_pagination_render(projects) }}
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
{% endblock %}
